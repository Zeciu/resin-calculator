// Legacy component retired in Task 35.
// The app now enters through the workspace shell, so this page intentionally
// renders no UI to avoid exposing a duplicate login experience.
export default function LandingPage() {
  return null;
}
