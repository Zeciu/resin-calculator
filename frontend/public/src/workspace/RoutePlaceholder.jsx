export default function RoutePlaceholder({ title }) {
  return (
    <div className="workspace-route-placeholder">
      <h2>{title}</h2>
      <p>Module content will be added in a later phase.</p>
    </div>
  );
}
