[project]
name = "resin-calculator-backend"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
    "python-jose[cryptography]>=3.3.0",
    "httpx>=0.28.1",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.5",
    "httpx>=0.28.1",
]